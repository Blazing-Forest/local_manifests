# Local Manifests 📦

```bash
git clone -b 16-qpr2 https://github.com/Blazing-Forest/local_manifests .repo/local_manifests
```

---

```bash
repo sync -c --force-sync --optimized-fetch --no-tags --no-clone-bundle --prune -j24
```

---